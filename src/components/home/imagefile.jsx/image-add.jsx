import React from 'react';

const ImageAdd = (props) => (
        <span className="pick">
            {/* <input type="text"/> */}
            <button >Pick image</button>
        </span>   
    );

export default ImageAdd;